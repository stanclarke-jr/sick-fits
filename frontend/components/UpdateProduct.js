const UpdateProduct = ({ id }) => {
  console.log('leave curlies');
  // 1. Get the existing product
  // 2. Get the mutation to update the product
  // 3. Form to handle the updates

  return (
    <div>
      <p>Update product id: {id}</p>
    </div>
  );
};

export default UpdateProduct;
